---
description: Test the skill tool
---

Use `skill` to load the `test-skill` skill.

Verify that:
1. The skill content is injected with XML structure (`<skill_content>`, `<skill_files>` tags)
2. The base directory and file list are included
3. The tool returns the skill content with available scripts listed
